# Raw data: ten 1.5 GB tif stacks (~14 GB). Not distributable via git.
# No trailing slash, so a symlink to a data drive is ignored too.
/mouse_data

# Regenerated by the notebooks
/seg_cache
/results/figures
/output_images

# Local environment and editor settings
/.venv
/.vscode

# OS / Python noise
.DS_Store
__pycache__/
*.pyc
.ipynb_checkpoints/
